import { test, expect } from '@playwright/test';

test.use({viewport:{width: 1920, height: 1080}});

test('test-1', async ({ page }) => {

  test.slow();
  await page.goto('https://awesomeyuer.github.io/JsonStructuresDiffAssistantToolsOnline/');
  await page.getByRole('button', { name: 'Next →' }).click();
  await new Promise(r => setTimeout(r, 1000));

  

  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));  
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowRight');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowLeft');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowLeft');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('ArrowLeft');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('Enter');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('Enter');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('Escape');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('Alt+s');
  await new Promise(r => setTimeout(r, 1000));
  await page.getByText('Loading ... press Esc for cancel waiting Json Structures Diff Assistant Tools On').press('Alt+z');

  await page.getByText('Side (z)').click();
  await page.getByLabel('Side (z)').press('Alt+o');
  await page.getByLabel('Side (z)').press('Alt+i');

  await page.keyboard.press("Alt+z");

  await new Promise(r => setTimeout(r, 5000));

});